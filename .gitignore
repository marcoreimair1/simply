.DS_Store

# Supabase CLI: Arbeitsdateien und die Kopie der Edge Function.
# Die Quelle ist monatsmail.ts im Wurzelverzeichnis — nur die wird versioniert,
# damit es keine zweite, abweichende Fassung gibt.
supabase/.temp/
supabase/.branches/
supabase/functions/

# Arbeitsordner des Editors, gehoert nicht ins Repo
.claude/

# Farbvorschau zum Veroeffentlichen — Wegwerfkopien, gehoeren nicht ins Repo
.vorschau/

# Nur fuer die Testreihen unter tests/. package.json und package-lock.json
# sind versioniert, damit ein "npm install" ueberall dieselben Fassungen holt.
node_modules/
